nop # no operation
push 69
pop
push 69 420 
swap
dup
add 1 # add, sub, div and mul have optional arguments. 
		   # if an argument is not provided it (adds || subtracts || divides || multiplies) the first two elements on the stack,
		   #																							   removes them and places the result at the top of the stack
		   # if an argument is provided it (adds || subtracts || divides || multiplies) to the last element of the stack
sub 1
over
cls
push 69 
div  23
mul 2
sub 4 
bitl 4
bitr 4 
cls
halt 0 # Exits with the exit code x
#jmp 0 # jumps to the first line (lines are numbered from 0)